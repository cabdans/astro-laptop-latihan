---
import BaseLayout from '../layouts/BaseLayout.astro';

// Data laptop — bersumber dari project ini saja, tanpa referensi luar
// gpuTgp: TGP GPU dalam watt (0 = tidak ada GPU dedicated)
// gpuVram: VRAM dalam GB (0 = tidak ada GPU dedicated)
// bobot: berat dalam kg (0 = data tidak tersedia)
// pmMulti: Passmark Multi-Thread score (0 = data tidak tersedia)
const laptops = [
  { slug:"produktivitas/lenovo-ideapad-slim3-14irh10-i5", nama:"Lenovo IdeaPad Slim 3 14IRH10 Core i5", harga:"Rp 10–11 Juta", hMin:10, hMax:11, kategori:"Produktivitas", cpu:"Intel Core i5-13420H", gpu:"Intel UHD Graphics", ram:"8GB DDR5 (upgradeable)", srgb:false, ramUp:true, gpuDed:false, lokal:false, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:16934 },
  { slug:"produktivitas/lenovo-ideapad-slim3-14arp10-r5", nama:"Lenovo IdeaPad Slim 3 14ARP10 Ryzen 5", harga:"Rp 10–11 Juta", hMin:10, hMax:11, kategori:"Produktivitas", cpu:"AMD Ryzen 5 7535HS", gpu:"Radeon 660M", ram:"8GB DDR5 (upgradeable)", srgb:false, ramUp:true, gpuDed:false, lokal:false, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:17651 },
  { slug:"produktivitas/advan-pixwar-touch-r7-8745hs", nama:"ADVAN Pixwar Touchscreen Ryzen 7 8745HS", harga:"Rp 9–10 Juta", hMin:9, hMax:10, kategori:"Produktivitas", cpu:"AMD Ryzen 7 8745HS", gpu:"Radeon 780M", ram:"16GB LPDDR5 (soldered)", srgb:false, ramUp:false, gpuDed:false, lokal:true, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:28675 },
  { slug:"produktivitas/advan-workplus-r7-7735hs", nama:"Advan Workplus Ryzen 7 7735HS", harga:"Rp 8–9 Juta", hMin:8, hMax:9, kategori:"Produktivitas", cpu:"AMD Ryzen 7 7735HS", gpu:"Radeon 680M", ram:"16GB LPDDR5 (soldered)", srgb:false, ramUp:false, gpuDed:false, lokal:true, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:22369 },
  { slug:"budget/advan-workmate-ultra-core-ultra5-115u", nama:"Advan Workmate Ultra Core Ultra 5 115U", harga:"Rp 7–8 Juta", hMin:7, hMax:8, kategori:"Budget", cpu:"Intel Core Ultra 5 115U", gpu:"Intel Arc Graphics", ram:"8GB DDR5 (upgradeable)", srgb:false, ramUp:true, gpuDed:false, lokal:true, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:12771 },
  { slug:"produktivitas/advan-workplus-r5-6600h", nama:"Advan Workplus Ryzen 5 6600H", harga:"Rp 7–8 Juta", hMin:7, hMax:8, kategori:"Produktivitas", cpu:"AMD Ryzen 5 6600H", gpu:"Radeon 660M", ram:"16GB LPDDR5 (soldered)", srgb:false, ramUp:false, gpuDed:false, lokal:true, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:18224 },
  { slug:"produktivitas/advan-workplus-heritage-r5-7535hs", nama:"Advan Workplus Heritage Ryzen 5 7535HS", harga:"Rp 7–8 Juta", hMin:7, hMax:8, kategori:"Produktivitas", cpu:"AMD Ryzen 5 7535HS", gpu:"Radeon 660M", ram:"8/16GB DDR5 (upgradeable)", srgb:false, ramUp:true, gpuDed:false, lokal:true, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:17651 },
  { slug:"produktivitas/asus-expertbook-pm1403cda-r5-150", nama:"ASUS ExpertBook PM1403CDA Ryzen 5 150", harga:"Rp 10–11 Juta", hMin:10, hMax:11, kategori:"Produktivitas", cpu:"AMD Ryzen 5 150U", gpu:"Radeon 740M", ram:"8/16GB DDR5 (upgradeable)", srgb:false, ramUp:true, gpuDed:false, lokal:false, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:15376 },
  { slug:"budget/polytron-luxia-pro-i5-1235u", nama:"Polytron Luxia Pro i5-1235U", harga:"Rp 8–9 Juta", hMin:8, hMax:9, kategori:"Budget", cpu:"Intel Core i5-1235U", gpu:"Intel Iris Xe", ram:"16GB LPDDR5 (soldered)", srgb:true, ramUp:false, gpuDed:false, lokal:true, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:12574 },
  { slug:"budget/lenovo-ideapad-slim3-14iru8-i3", nama:"Lenovo IdeaPad Slim 3 14IRU8 Core i3", harga:"Rp 7–8 Juta", hMin:7, hMax:8, kategori:"Budget", cpu:"Intel Core i3-1315U", gpu:"Intel UHD Graphics", ram:"8GB LPDDR5 (soldered)", srgb:false, ramUp:false, gpuDed:false, lokal:false, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:11109 },
  { slug:"produktivitas/polytron-luxia-pro-ultra5-125h", nama:"Polytron Luxia Pro Ultra 5 125H", harga:"Rp 10–11 Juta", hMin:10, hMax:11, kategori:"Produktivitas", cpu:"Intel Core Ultra 5 125H", gpu:"Intel Arc Graphics", ram:"16GB LPDDR5X (soldered)", srgb:true, ramUp:false, gpuDed:false, lokal:true, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:20314 },
  { slug:"produktivitas/acer-aspire-go-14-ultra5-125h", nama:"Acer Aspire Go 14 Ai Ultra 5 125H", harga:"Rp 10–12 Juta", hMin:10, hMax:12, kategori:"Produktivitas", cpu:"Intel Core Ultra 5 125H", gpu:"Intel Arc Graphics", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:false, lokal:false, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:20314 },
  { slug:"produktivitas/acer-aspire-lite-15-r7-8840hs", nama:"Acer Aspire Lite 15 Ryzen 7 8840HS", harga:"Rp 11–12 Juta", hMin:11, hMax:12, kategori:"Produktivitas", cpu:"AMD Ryzen 7 8840HS", gpu:"Radeon 780M", ram:"16GB DDR (upgradeable)", srgb:false, ramUp:true, gpuDed:false, lokal:false, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:24807 },
  { slug:"gaming/asus-tuf-a16-fa608uh-rtx5050", nama:"ASUS TUF A16 FA608UH RTX 5050", harga:"Rp 21–22 Juta", hMin:21, hMax:22, kategori:"Gaming", cpu:"AMD Ryzen AI 7 260", gpu:"RTX 5050 8GB", ram:"DDR5 SO-DIMM (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:115, gpuVram:8, bobot:0, pmMulti:28167 },
  { slug:"gaming/axioo-pongo-765", nama:"Axioo Pongo 765 i7-13620H RTX 5060", harga:"Rp 22–23 Juta", hMin:22, hMax:23, kategori:"High Gaming", cpu:"Intel Core i7-13620H", gpu:"RTX 5060 8GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:true, gpuTgp:115, gpuVram:8, bobot:2.35, pmMulti:23398 },
  { slug:"gaming/axioo-pongo-765-v2", nama:"Axioo Pongo 765 V2 i7-13700H RTX 5060", harga:"Rp 22–23 Juta", hMin:22, hMax:23, kategori:"High Gaming", cpu:"Intel Core i7-13700H", gpu:"RTX 5060 8GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:true, gpuTgp:115, gpuVram:8, bobot:2.35, pmMulti:25960 },
  { slug:"gaming/lenovo-loq-15ahp11", nama:"Lenovo LOQ 15AHP11 R7 250 RTX 5050", harga:"Rp 23–24 Juta", hMin:23, hMax:24, kategori:"Gaming", cpu:"AMD Ryzen AI 7 250", gpu:"RTX 5050 8GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:115, gpuVram:8, bobot:2.1, pmMulti:25331 },
  { slug:"gaming/axioo-pongo-755-x72", nama:"Axioo Pongo 755-X72 R7 255 RTX 5050", harga:"Rp 19–20 Juta", hMin:19, hMax:20, kategori:"Gaming", cpu:"AMD Ryzen AI 7 255", gpu:"RTX 5050 8GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:true, gpuTgp:135, gpuVram:8, bobot:2.35, pmMulti:28774 },
  { slug:"gaming/axioo-pongo-755-r7255", nama:"Axioo Pongo 755 R7 255 RTX 5050", harga:"Rp 18–19 Juta", hMin:18, hMax:19, kategori:"Gaming", cpu:"AMD Ryzen AI 7 255", gpu:"RTX 5050 8GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:true, gpuTgp:115, gpuVram:8, bobot:2.35, pmMulti:28774 },
  { slug:"gaming/lenovo-loq-15ahp10", nama:"Lenovo LOQ 15AHP10 RTX 5050", harga:"Rp 20 Juta", hMin:20, hMax:20, kategori:"Gaming", cpu:"AMD Ryzen AI 7 250", gpu:"RTX 5050 8GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:100, gpuVram:8, bobot:2.3, pmMulti:25331 },
  { slug:"gaming/lenovo-loq-15irx10-i7", nama:"Lenovo LOQ 15IRX10 i7-13700HX RTX 5050", harga:"Rp 25–26 Juta", hMin:25, hMax:26, kategori:"Gaming", cpu:"Intel Core i7-13700HX", gpu:"RTX 5050 8GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:100, gpuVram:8, bobot:2.4, pmMulti:31788 },
  { slug:"gaming/lenovo-loq-15irx10", nama:"Lenovo LOQ 15IRX10 i5-13450HX RTX 5050", harga:"Rp 22–23 Juta", hMin:22, hMax:23, kategori:"Gaming", cpu:"Intel Core i5-13450HX", gpu:"RTX 5050 8GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:105, gpuVram:8, bobot:2.4, pmMulti:24682 },
  { slug:"gaming/asus-gaming-v16-v3607vh", nama:"ASUS Gaming V16 V3607VH Core 5 210H", harga:"Rp 16–17 Juta", hMin:16, hMax:17, kategori:"Gaming", cpu:"Intel Core 5 210H", gpu:"RTX 5050 8GB", ram:"16GB DDR5 (upgradeable)", srgb:false, ramUp:true, gpuDed:true, lokal:false, gpuTgp:75, gpuVram:8, bobot:1.95, pmMulti:18310 },
  { slug:"gaming/hp-victus-15-fa2717tx", nama:"HP Victus 15 FA2717TX i5-13420H", harga:"Rp 17–18 Juta", hMin:17, hMax:18, kategori:"Gaming", cpu:"Intel Core i5-13420H", gpu:"RTX 4050 6GB", ram:"16GB DDR4 (upgradeable)", srgb:false, ramUp:true, gpuDed:true, lokal:false, gpuTgp:50, gpuVram:6, bobot:2.29, pmMulti:16934 },
  { slug:"gaming/lenovo-loq-essential-15arp10e", nama:"Lenovo LOQ Essential 15ARP10E R7", harga:"Rp 16–17 Juta", hMin:16, hMax:17, kategori:"Gaming", cpu:"AMD Ryzen 7 7735HS", gpu:"RTX 4050 6GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:65, gpuVram:6, bobot:1.80, pmMulti:22369 },
  { slug:"gaming/colorful-evol-p15-he55d", nama:"Colorful Evol P15 HE55D i5-12450H", harga:"Rp 15–16 Juta", hMin:15, hMax:16, kategori:"Gaming", cpu:"Intel Core i5-12450H", gpu:"RTX 4050 6GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:115, gpuVram:6, bobot:0, pmMulti:14600 },
  { slug:"gaming/lenovo-loq-15irx9", nama:"Lenovo LOQ 15IRX9 i5-13450HX", harga:"Rp 15–16 Juta", hMin:15, hMax:16, kategori:"Gaming", cpu:"Intel Core i5-13450HX", gpu:"RTX 3050 6GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:95, gpuVram:6, bobot:2.38, pmMulti:24682 },
  { slug:"gaming/acer-nitro-v15-5050", nama:"Acer Nitro V15 RTX 5050 i5", harga:"Rp 17–18 Juta", hMin:17, hMax:18, kategori:"Gaming", cpu:"Intel Core i5-13420H", gpu:"RTX 5050 8GB", ram:"16GB DDR4 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:75, gpuVram:8, bobot:2.1, pmMulti:16934 },
  { slug:"gaming/acer-nitro-v15-anv15-42", nama:"Acer Nitro V15 ANV15-42 R7", harga:"Rp 17–18 Juta", hMin:17, hMax:18, kategori:"Gaming", cpu:"AMD Ryzen 7 7445HS", gpu:"RTX 4050 6GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:60, gpuVram:6, bobot:2.1, pmMulti:18718 },
  { slug:"gaming/colorful-evol-p15-rtx5050", nama:"Colorful Evol P15 RTX 5050", harga:"Rp 15–16 Juta", hMin:15, hMax:16, kategori:"Gaming", cpu:"Intel Core i5-13420H", gpu:"RTX 5050 8GB", ram:"DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:115, gpuVram:8, bobot:2.25, pmMulti:16934 },
  { slug:"gaming/acer-nitro-lite-nl16", nama:"Acer Nitro Lite NL16", harga:"Rp 14–15 Juta", hMin:14, hMax:15, kategori:"Gaming", cpu:"Intel Core 5 210H", gpu:"RTX 3050 6GB", ram:"16GB DDR5 (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:55, gpuVram:6, bobot:1.95, pmMulti:18310 },
  { slug:"gaming/acer-aspire-7-pro-a715", nama:"Acer Aspire 7 Pro A715", harga:"Rp 12–13 Juta", hMin:12, hMax:13, kategori:"Gaming", cpu:"Intel Core i5-13420H", gpu:"RTX 3050 6GB", ram:"16GB DDR4 (upgradeable)", srgb:false, ramUp:true, gpuDed:true, lokal:false, gpuTgp:75, gpuVram:6, bobot:2.1, pmMulti:16934 },
  { slug:"gaming/asus-tuf-gaming-a16", nama:"ASUS TUF Gaming A16", harga:"Rp 16–17 Juta", hMin:16, hMax:17, kategori:"Gaming", cpu:"AMD Ryzen 7 7445HS", gpu:"RTX 4050 6GB", ram:"DDR5 SO-DIMM (upgradeable)", srgb:true, ramUp:true, gpuDed:true, lokal:false, gpuTgp:115, gpuVram:6, bobot:2.2, pmMulti:18718 },
  { slug:"budget/asus-vivobook-15", nama:"ASUS VivoBook 15", harga:"Rp 7–8 Juta", hMin:7, hMax:8, kategori:"Budget", cpu:"AMD Ryzen 5 7520U", gpu:"Radeon 610M", ram:"8GB DDR5 (upgradeable)", srgb:false, ramUp:true, gpuDed:false, lokal:false, gpuTgp:0, gpuVram:0, bobot:1.7, pmMulti:8949 },
  { slug:"produktivitas/acer-aspire-lite-15-r5-8640hs", nama:"Acer Aspire Lite 15 R5-8640HS", harga:"Rp 9–10 Juta", hMin:9, hMax:10, kategori:"Produktivitas", cpu:"AMD Ryzen 5 8640HS", gpu:"Radeon 760M", ram:"8GB DDR5 (upgradeable)", srgb:false, ramUp:true, gpuDed:false, lokal:false, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:19897 },
  { slug:"produktivitas/acer-aspire-lite-15-r5-7430u", nama:"Acer Aspire Lite 15 R5-7430U", harga:"Rp 7–8 Juta", hMin:7, hMax:8, kategori:"Produktivitas", cpu:"AMD Ryzen 5 7430U", gpu:"Radeon 610M", ram:"8GB DDR4 (upgradeable)", srgb:false, ramUp:true, gpuDed:false, lokal:false, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:15484 },
  { slug:"budget/acer-aspire-go-14-i3-1315u", nama:"ACER Aspire GO 14 i3-1315U", harga:"Rp 7–8 Juta", hMin:7, hMax:8, kategori:"Budget", cpu:"Intel Core i3-1315U", gpu:"Intel UHD Graphics", ram:"8GB DDR4 (upgradeable)", srgb:false, ramUp:true, gpuDed:false, lokal:false, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:11109 },
  { slug:"budget/acer-aspire-lite-al14-42p", nama:"ACER Aspire Lite AL14-42P", harga:"Rp 6–7 Juta", hMin:6, hMax:7, kategori:"Budget", cpu:"AMD Ryzen 3 5300U", gpu:"Radeon Graphics", ram:"8GB DDR4 (upgradeable)", srgb:false, ramUp:true, gpuDed:false, lokal:false, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:9317 },
  { slug:"budget/polytron-luxia-i3-1215", nama:"POLYTRON Laptop Luxia i3 1215", harga:"Rp 6–7 Juta", hMin:6, hMax:7, kategori:"Budget", cpu:"Intel Core i3-1215U", gpu:"Intel UHD Graphics", ram:"8GB DDR4 (upgradeable)", srgb:false, ramUp:true, gpuDed:false, lokal:true, gpuTgp:0, gpuVram:0, bobot:0, pmMulti:10173 },
];
---

<BaseLayout title="Temukan Laptop — AstroLaptop" description="Filter wizard 5 langkah untuk menemukan laptop paling sesuai budget dan kebutuhanmu">
<style>
  .rekomen-hero {
    text-align: center;
    padding: 48px 20px 32px;
  }
  .rekomen-hero h1 {
    font-size: clamp(1.6rem, 4vw, 2.4rem);
    font-weight: 800;
    background: linear-gradient(135deg, #e2e8f0, #93c5fd, #c4b5fd);
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin: 0 0 10px;
  }
  .rekomen-hero p {
    color: var(--text-muted);
    font-size: 1rem;
    margin: 0;
  }

  /* Filter wizard */
  .filter-wizard {
    max-width: 780px;
    margin: 0 auto 48px;
    padding: 0 16px;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }
  .filter-bar {
    background: rgba(10,10,42,0.85);
    border: 1px solid rgba(96,165,250,0.15);
    border-radius: 14px;
    padding: 20px 22px;
    transition: border-color 0.3s;
  }
  .filter-bar.has-selection {
    border-color: rgba(96,165,250,0.4);
  }
  .filter-label {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 0.92rem;
    font-weight: 600;
    color: var(--text);
    margin-bottom: 14px;
  }
  .filter-num {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 24px;
    height: 24px;
    background: rgba(96,165,250,0.15);
    border: 1px solid rgba(96,165,250,0.3);
    border-radius: 50%;
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--primary);
    flex-shrink: 0;
  }
  .filter-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  .chip {
    padding: 8px 16px;
    border-radius: 999px;
    border: 1px solid rgba(255,255,255,0.12);
    background: rgba(255,255,255,0.04);
    color: var(--text-muted);
    font-size: 0.84rem;
    cursor: pointer;
    transition: all 0.2s ease;
    white-space: nowrap;
  }
  .chip:hover {
    border-color: rgba(96,165,250,0.4);
    color: var(--text);
    background: rgba(96,165,250,0.08);
  }
  .chip.active {
    background: var(--primary);
    border-color: var(--primary);
    color: #02020e;
    font-weight: 700;
    box-shadow: 0 0 16px rgba(96,165,250,0.4);
  }
  .reset-btn {
    padding: 9px 24px;
    border-radius: 999px;
    border: 1px solid rgba(255,255,255,0.15);
    background: transparent;
    color: var(--text-muted);
    font-size: 0.84rem;
    cursor: pointer;
    transition: all 0.2s;
  }
  .reset-btn:hover {
    color: var(--text);
    border-color: rgba(255,255,255,0.3);
  }

  /* Results */
  .results-section {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 16px 64px;
  }
  .results-header {
    font-size: 0.95rem;
    color: var(--text-muted);
    margin-bottom: 20px;
    padding-left: 4px;
  }
  .results-header strong {
    color: var(--primary);
    font-weight: 700;
  }
  .fallback-note {
    display: inline-block;
    margin-bottom: 16px;
    padding: 8px 16px;
    background: rgba(251,191,36,0.08);
    border: 1px solid rgba(251,191,36,0.25);
    border-radius: 8px;
    color: #fbbf24;
    font-size: 0.84rem;
  }
  .results-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }

  @media (max-width: 900px) {
    .results-grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 600px) {
    .filter-bar { padding: 16px; }
    .chip { font-size: 0.8rem; padding: 7px 13px; }
    .results-grid { grid-template-columns: 1fr 1fr; gap: 10px; }
  }
  @media (max-width: 400px) {
    .results-grid { grid-template-columns: 1fr; }
  }

  /* ── LIGHT MODE OVERRIDES ────────────────────────────────── */
  :global(html[data-theme="light"]) .filter-bar {
    background: rgba(255,255,255,0.88);
    border-color: rgba(37,99,235,0.15);
    box-shadow: 0 2px 14px rgba(0,0,0,0.07);
  }
  :global(html[data-theme="light"]) .filter-bar.has-selection {
    border-color: rgba(37,99,235,0.4);
  }
  :global(html[data-theme="light"]) .chip {
    border-color: rgba(0,0,0,0.14);
    background: rgba(0,0,0,0.04);
    color: #475569;
  }
  :global(html[data-theme="light"]) .chip:hover {
    border-color: rgba(37,99,235,0.35);
    background: rgba(37,99,235,0.07);
    color: #2563eb;
  }
  :global(html[data-theme="light"]) .chip.active {
    background: #2563eb;
    border-color: #2563eb;
    color: #fff;
  }
  :global(html[data-theme="light"]) .reset-btn {
    border-color: rgba(0,0,0,0.18);
    color: #475569;
  }
  :global(html[data-theme="light"]) .reset-btn:hover {
    border-color: rgba(37,99,235,0.4);
    color: #2563eb;
  }
  :global(html[data-theme="light"]) .rekomen-hero h1 {
    background: linear-gradient(135deg, #1e293b, #2563eb, #7c3aed);
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
</style>

<div class="rekomen-hero reveal">
  <h1>🎯 Temukan Laptop yang Pas</h1>
  <p>Jawab 5 pertanyaan singkat — kami carikan laptop terbaik dari 39 pilihan</p>
</div>

<div class="filter-wizard">

  <!-- Bar 1: Budget -->
  <div class="filter-bar reveal" id="bar-budget">
    <div class="filter-label">
      <span class="filter-num">1</span>
      💰 Berapa budget kamu?
    </div>
    <div class="filter-chips" data-group="budget">
      <button class="chip" data-val="under5">Di bawah Rp 5 juta</button>
      <button class="chip" data-val="5to10">Rp 5–10 juta</button>
      <button class="chip" data-val="10to15">Rp 10–15 juta</button>
      <button class="chip" data-val="15to20">Rp 15–20 juta</button>
      <button class="chip" data-val="over20">Di atas Rp 20 juta</button>
    </div>
  </div>

  <!-- Bar 2: RAM -->
  <div class="filter-bar reveal" id="bar-ram">
    <div class="filter-label">
      <span class="filter-num">2</span>
      🔧 Perlu RAM upgradeable?
    </div>
    <div class="filter-chips" data-group="ram">
      <button class="chip" data-val="any">Tidak perlu</button>
      <button class="chip" data-val="want">Pengen upgradeable</button>
      <button class="chip" data-val="must">Harus upgradeable</button>
    </div>
  </div>

  <!-- Bar 3: Editing -->
  <div class="filter-bar reveal" id="bar-editing">
    <div class="filter-label">
      <span class="filter-num">3</span>
      🎨 Kebutuhan editing?
    </div>
    <div class="filter-chips" data-group="editing">
      <button class="chip" data-val="none">Tidak ada</button>
      <button class="chip" data-val="light">Editing ringan (foto/Canva)</button>
      <button class="chip" data-val="heavy">Editing berat (video/3D)</button>
    </div>
  </div>

  <!-- Bar 4: Performance -->
  <div class="filter-bar reveal" id="bar-performa">
    <div class="filter-label">
      <span class="filter-num">4</span>
      ⚡ Prioritas performa?
    </div>
    <div class="filter-chips" data-group="performa">
      <button class="chip" data-val="ringan">🪶 Ringan & Awet Baterai</button>
      <button class="chip" data-val="cpu">🖥️ CPU</button>
      <button class="chip" data-val="berat">🔥 Berat</button>
      <button class="chip" data-val="gpu">🎮 GPU</button>
    </div>
  </div>

  <!-- Bar 5: Brand -->
  <div class="filter-bar reveal" id="bar-brand">
    <div class="filter-label">
      <span class="filter-num">5</span>
      🏷️ Preferensi brand?
    </div>
    <div class="filter-chips" data-group="brand">
      <button class="chip" data-val="all">Semua brand</button>
      <button class="chip" data-val="lokal">Brand lokal/murah</button>
      <button class="chip" data-val="luar">Brand luar (Asus/Acer/Lenovo/HP)</button>
    </div>
  </div>

  <div style="text-align:center; margin-top:4px;">
    <button class="reset-btn" id="resetBtn">↺ Reset Pilihan</button>
  </div>
</div>

<!-- Results -->
<div class="results-section">
  <div id="resultsHeader" class="results-header"></div>
  <div id="fallbackNote" style="display:none;">
    <span class="fallback-note">⚠️ Tidak ada laptop yang 100% cocok. Ini yang paling mendekati kebutuhanmu:</span>
  </div>
  <div id="resultsGrid" class="results-grid"></div>
</div>

<script define:vars={{ laptops }}>
  // State
  const state = { budget: null, ram: null, editing: null, performa: null, brand: null };

  // Filter logic
  function budgetPass(l, val) {
    if (!val) return true;
    if (l.hMin === 0) return true;
    if (val === 'under5')  return l.hMax <= 5;
    if (val === '5to10')   return l.hMin >= 5  && l.hMax <= 10;
    if (val === '10to15')  return l.hMin >= 10 && l.hMax <= 15;
    if (val === '15to20')  return l.hMin >= 15 && l.hMax <= 20;
    if (val === 'over20')  return l.hMin >= 20;
    return true;
  }

  function passesAllFilters(l) {
    if (!budgetPass(l, state.budget)) return false;
    if ((state.ram === 'want' || state.ram === 'must') && !l.ramUp) return false;
    if (state.editing === 'light' && !l.srgb) return false;
    if (state.editing === 'heavy' && (!l.srgb || !l.gpuDed)) return false;
    // Bar 4: Prioritas performa
    if (state.performa === 'ringan' && l.gpuDed) return false;
    if (state.performa === 'gpu'    && !l.gpuDed) return false;
    // 'cpu' dan 'berat': tidak ada hard filter tambahan
    if (state.brand === 'lokal' && !l.lokal) return false;
    if (state.brand === 'luar' && l.lokal) return false;
    return true;
  }

  function scoreClosest(l) {
    let s = 0;
    if (budgetPass(l, state.budget)) s += 2;
    if (!state.ram || state.ram === 'any' || l.ramUp) s++;
    if (!state.editing || state.editing === 'none') s++;
    else if (state.editing === 'light' && l.srgb) s++;
    else if (state.editing === 'heavy' && l.srgb && l.gpuDed) s++;
    if (!state.performa || state.performa === 'cpu') s++;
    else if (state.performa === 'ringan' && !l.gpuDed) s++;
    else if (state.performa === 'gpu' && l.gpuDed) s++;
    else if (state.performa === 'berat' && l.pmMulti >= 18000) s++;
    if (!state.brand || state.brand === 'all') s++;
    else if (state.brand === 'lokal' && l.lokal) s++;
    else if (state.brand === 'luar' && !l.lokal) s++;
    return s;
  }

  // Sort hasil berdasarkan pilihan Bar 4
  /* Efficiency score untuk mode Ringan & Awet Baterai.
     Komponen (semua dari data yang sudah ada di project):
     - Tidak ada GPU dedicated (+60)  → hemat daya paling signifikan
     - GPU TGP tinggi (-0.45/Watt)    → makin boros listrik, makin rendah
     - Bobot ringan (+12 per 0.1kg)   → chassis kecil biasanya baterai lebih awet
     - pmMulti tinggi (+1/2000)       → proxy efisiensi CPU (performa per watt)
  */
  function efficiencyScore(l) {
    let s = 0;
    if (!l.gpuDed)    s += 60;
    if (l.gpuTgp > 0) s -= l.gpuTgp * 0.45;
    if (l.bobot > 0)  s += (3.2 - l.bobot) * 12;
    if (l.pmMulti > 0) s += l.pmMulti / 2000;
    return s;
  }

  function sortByPerforma(list) {
    const p = state.performa;
    if (p === 'gpu') {
      // GPU mode: TGP tertinggi → VRAM terbesar
      return [...list].sort((a, b) =>
        b.gpuTgp !== a.gpuTgp ? b.gpuTgp - a.gpuTgp : b.gpuVram - a.gpuVram
      );
    }
    if (p === 'cpu') {
      // CPU mode: Passmark Multi-Thread tertinggi
      return [...list].sort((a, b) => b.pmMulti - a.pmMulti);
    }
    if (p === 'berat') {
      // Berat mode: gabungan CPU + GPU — cocok untuk beban berat (render, gaming+stream)
      return [...list].sort((a, b) =>
        (b.pmMulti + b.gpuTgp * 100) - (a.pmMulti + a.gpuTgp * 100)
      );
    }
    if (p === 'ringan') {
      // Ringan mode: efficiency score tertinggi (no-dGPU bonus, TGP penalty, bobot, pmMulti)
      return [...list].sort((a, b) => efficiencyScore(b) - efficiencyScore(a));
    }
    return list;
  }

  function hasAnyFilter() {
    return state.budget || state.ram || state.editing || state.performa || state.brand;
  }

  // Warna aksen per kategori
  const katColor = {
    'Budget':       '#60a5fa',
    'Produktivitas':'#a78bfa',
    'Gaming':       '#f97316',
    'High Gaming':  '#ef4444',
  };

  // Deteksi tema aktif (light/dark) untuk inline styles card yang theme-aware
  function isLightTheme() {
    return document.documentElement.getAttribute('data-theme') === 'light';
  }

  // Build card HTML — fully inline styles (Astro scoped CSS tidak berlaku pada innerHTML)
  // FIX P1: warna inline kini menyesuaikan light/dark mode (sebelumnya selalu dark).
  function buildCard(l) {
    const accent = katColor[l.kategori] || '#60a5fa';
    const light = isLightTheme();
    // Token warna per tema
    const cardBg      = light ? 'rgba(255,255,255,0.92)' : 'rgba(10,10,42,0.85)';
    const cardBorder  = light ? 'rgba(0,0,0,0.1)'        : 'rgba(96,165,250,0.15)';
    const nameColor   = light ? '#1e293b'                : '#e2e8f0';
    const specColor   = light ? '#475569'                : '#94a3b8';
    const footerLine  = light ? 'rgba(0,0,0,0.08)'       : 'rgba(255,255,255,0.06)';
    const seeColor    = light ? '#64748b'                : '#64748b';
    const cardShadow  = light ? 'box-shadow:0 2px 12px rgba(0,0,0,0.07);' : '';

    const badges = [];
    if (l.srgb)   badges.push(`<span style="display:inline-block;font-size:0.68rem;font-weight:600;padding:2px 8px;border-radius:4px;background:rgba(52,211,153,0.15);color:${light ? '#059669' : '#34d399'};border:1px solid rgba(52,211,153,0.3);">✓ 100% sRGB</span>`);
    if (l.gpuDed) badges.push(`<span style="display:inline-block;font-size:0.68rem;font-weight:600;padding:2px 8px;border-radius:4px;background:rgba(167,139,250,0.15);color:${light ? '#7c3aed' : '#a78bfa'};border:1px solid rgba(167,139,250,0.3);">GPU Dedicated</span>`);
    if (l.ramUp)  badges.push(`<span style="display:inline-block;font-size:0.68rem;font-weight:600;padding:2px 8px;border-radius:4px;background:rgba(251,191,36,0.12);color:${light ? '#b45309' : '#fbbf24'};border:1px solid rgba(251,191,36,0.25);">RAM Upgradeable</span>`);
    if (l.lokal)  badges.push(`<span style="display:inline-block;font-size:0.68rem;font-weight:600;padding:2px 8px;border-radius:4px;background:rgba(249,115,22,0.12);color:${light ? '#c2410c' : '#fb923c'};border:1px solid rgba(249,115,22,0.25);">Brand Lokal</span>`);

    const hargaColor = l.hMin === 0 ? (light ? '#d97706' : '#fbbf24') : (light ? '#2563eb' : '#60a5fa');

    return `
      <a href="/review/${l.slug}" style="
        display:flex;flex-direction:column;
        background:${cardBg};
        border:1px solid ${cardBorder};
        border-top:3px solid ${accent};
        border-radius:14px;overflow:hidden;
        text-decoration:none;${cardShadow}
        transition:border-color 0.25s,transform 0.25s;
      " onmouseover="this.style.borderColor='${accent}';this.style.transform='translateY(-3px)'"
         onmouseout="this.style.borderColor='${cardBorder}';this.style.transform='none'">
        <div style="padding:14px 16px 16px;display:flex;flex-direction:column;gap:10px;flex:1;">

          <!-- Kategori -->
          <div style="display:flex;align-items:center;justify-content:space-between;">
            <span style="font-size:0.7rem;font-weight:700;text-transform:uppercase;letter-spacing:0.06em;color:${accent};">${l.kategori}</span>
          </div>

          <!-- Nama laptop -->
          <div style="font-size:0.9rem;font-weight:700;color:${nameColor};line-height:1.35;">${l.nama}</div>

          <!-- Spesifikasi -->
          <div style="display:flex;flex-direction:column;gap:4px;">
            <div style="font-size:0.76rem;color:${specColor};">🖥️ ${l.cpu}</div>
            <div style="font-size:0.76rem;color:${specColor};">🎮 ${l.gpu}</div>
            <div style="font-size:0.76rem;color:${specColor};">💾 ${l.ram}</div>
            ${state.performa === 'gpu' && l.gpuTgp ? `<div style="font-size:0.76rem;color:${light ? '#ea580c' : '#f97316'};">⚡ TGP: ${l.gpuTgp}W · VRAM: ${l.gpuVram}GB</div>` : ''}
            ${state.performa === 'cpu' && l.pmMulti ? `<div style="font-size:0.76rem;color:${light ? '#2563eb' : '#60a5fa'};">🖥️ Passmark Multi: ${l.pmMulti.toLocaleString('id-ID')}</div>` : ''}
            ${state.performa === 'berat' && l.pmMulti ? `<div style="font-size:0.76rem;color:${light ? '#7c3aed' : '#a78bfa'};">📊 CPU+GPU Score: ${(l.pmMulti + l.gpuTgp * 100).toLocaleString('id-ID')}</div>` : ''}
            ${state.performa === 'ringan' ? `<div style="font-size:0.76rem;color:${light ? '#059669' : '#34d399'};">${!l.gpuDed ? '🔋 Tanpa dGPU' : ''}${l.bobot ? (l.gpuDed ? '' : ' · ') + '⚖️ ' + l.bobot + ' kg' : ''}${l.pmMulti ? ' · eff: ' + Math.round(efficiencyScore(l)) : ''}</div>` : ''}
          </div>

          <!-- Badges -->
          ${badges.length ? `<div style="display:flex;flex-wrap:wrap;gap:5px;">${badges.join('')}</div>` : ''}

          <!-- Footer: harga + link -->
          <div style="display:flex;align-items:center;justify-content:space-between;margin-top:auto;padding-top:8px;border-top:1px solid ${footerLine};">
            <span style="font-size:0.83rem;font-weight:700;color:${hargaColor};">${l.harga}</span>
            <span style="font-size:0.75rem;color:${seeColor};">Lihat review →</span>
          </div>

        </div>
      </a>`;
  }

  function render() {
    const header = document.getElementById('resultsHeader');
    const grid = document.getElementById('resultsGrid');
    const fallbackNote = document.getElementById('fallbackNote');

    if (!hasAnyFilter()) {
      header.innerHTML = `Pilih filter di atas untuk menemukan laptop yang pas.`;
      fallbackNote.style.display = 'none';
      grid.innerHTML = '';
      return;
    }

    const matched = laptops.filter(passesAllFilters);

    if (matched.length > 0) {
      fallbackNote.style.display = 'none';
      header.innerHTML = `<strong>${matched.length} laptop</strong> cocok dengan pilihanmu`;
      grid.innerHTML = sortByPerforma(matched).map(l => buildCard(l)).join('');
    } else {
      // Fallback: tampilkan 3 laptop dengan skor tertinggi
      const scored = laptops
        .map(l => ({ ...l, _score: scoreClosest(l) }))
        .sort((a, b) => b._score - a._score)
        .slice(0, 3);

      fallbackNote.style.display = 'block';
      header.innerHTML = `Tidak ada laptop yang cocok 100% — menampilkan <strong>${scored.length} yang paling mendekati</strong>`;
      grid.innerHTML = scored.map(l => buildCard(l)).join('');
    }
  }

  // Chip click handling
  document.querySelectorAll('.filter-chips').forEach(group => {
    const groupName = group.dataset.group;
    group.querySelectorAll('.chip').forEach(chip => {
      chip.addEventListener('click', () => {
        // Toggle: kalau sudah aktif → deselect
        const isActive = chip.classList.contains('active');
        group.querySelectorAll('.chip').forEach(c => c.classList.remove('active'));
        const bar = group.closest('.filter-bar');
        if (isActive) {
          state[groupName] = null;
          bar.classList.remove('has-selection');
        } else {
          chip.classList.add('active');
          state[groupName] = chip.dataset.val;
          bar.classList.add('has-selection');
        }
        render();
      });
    });
  });

  // Reset
  document.getElementById('resetBtn').addEventListener('click', () => {
    Object.keys(state).forEach(k => state[k] = null);
    document.querySelectorAll('.chip').forEach(c => c.classList.remove('active'));
    document.querySelectorAll('.filter-bar').forEach(b => b.classList.remove('has-selection'));
    render();
  });

  // Re-render hasil saat tema berubah agar warna card (inline styles) ikut menyesuaikan
  new MutationObserver(() => {
    if (hasAnyFilter()) render();
  }).observe(document.documentElement, { attributes: true, attributeFilter: ['data-theme'] });

  // Init
  render();
</script>
</BaseLayo